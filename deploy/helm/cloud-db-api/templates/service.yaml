apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-db-api.fullname" . }}
  labels:
    {{- include "cloud-db-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cloud-db-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
