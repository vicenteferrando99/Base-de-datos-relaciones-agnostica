cloud-db-api desplegada.

Proveedor activo: {{ .Values.provider }}

Para acceder a la UI y la API desde tu máquina:

  kubectl port-forward svc/{{ include "cloud-db-api.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

y abre http://localhost:{{ .Values.service.port }}  (Swagger: /docs)

Estado del pod:

  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}
{{- if not .Values.dockerSocket.enabled }}

AVISO: dockerSocket.enabled=false — el adaptador local_docker no funcionará
dentro del cluster (los proveedores cloud sí, con sus Secrets configurados).
{{- end }}
